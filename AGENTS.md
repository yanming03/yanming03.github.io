# 网站设计规则

每次更新，向我确认后推送到远端。所有页面与组件必须遵守以下规则。

## 1. 视觉

- 只使用黑、白、灰。
- 数学公式是颜色规则的唯一例外：默认使用蓝色，可按内容需要选择蓝、红、绿、紫或橙色，不使用黑色公式。
- 每种语言只使用一种 8-bit 字体：英文使用 `Pixelify Sans`，简体中文使用 `Fusion Pixel 12px`；禁止同一语言混用字体。
- 不使用渐变、阴影、拟物、圆角卡片和大面积色块。
- 使用字符、留白、缩进和细线建立层级。
- 虚线表示弱关系，实线表示确定关系。
- 装饰图形必须表达关系；否则删除。

## 2. 页面结构

- 首页只包含：身份、关系导航、内容索引、联系方式。
- research 下的 blogs、agenda、papers 与 misc 使用同一内容流，不建立封闭专区。
- 每个索引条目只显示：编号、类型、标题、年份、链接。
- 新内容向下追加，不重排已有结构。
- 移动端保持相同顺序，仅减少次要信息。

## 3. 符号

- 优先使用：`[ ] / :: -> + * _ ...`。
- 箭头表示跳转或映射。
- 垂直线表示从属，水平线表示并列。
- 每屏最多使用一个无功能的趣味符号。
- 借用文本界面语法，不复制完整 Vim 界面。

## 4. 交互

- 悬停时只增加 `>`、下划线或光标。
- 动画使用离散步进，时长不超过 200ms。
- 不使用弹窗、浮动卡片和缩放动画。
- 详情在原位展开或进入独立纯文本页面。
- 键盘操作与清晰焦点状态必须可用。

## 5. 内容
- 保持简洁。
- 文案尽量短；首页不写解释性长段落。
- 技术论断按“观察事实 -> 成立边界 -> 可检验后果”组织；缺少证据时写成假设或问题。
- 删除训诫式和管理式文案；不使用“应沉淀”“不要记忆”“赋能”等空泛命令。
- 每句话只承担一个判断，并写明产生结果的对象或机制。
- 使用具体动词与直接指代；删除“事实成立的环境边界”“错误分型绑定失败边”一类抽象名词链。
- 公式已经表达关系时，正文只写它带来的操作后果，不重复翻译公式。
- 示例只承担概念映射，不增加故事；优先用交换图、流程图和表格表达关系，正文只解释图中无法直接读出的信息。
- 数学公式使用 KaTeX，默认使用块级公式，不使用行内公式；用 `data-math-color` 选择公式颜色。
- 图片、视频、音频和文本在首页统一显示为文件条目。
- 富媒体只在详情页展开。
- 内容数据至少包含：`id`、`type`、`title`、`year`、`href`。

### 博文写作通则

- 用户口述是原始稿。先提取中心命题、论证顺序、例子与结论，再开始改写。
- 保留口述的原本语义、立场和论证逻辑；删除“嗯”等停顿、重复和口误，不擅自补出新的主张。
- 口述存在会改变论证的歧义时，保留明确的待确认项或向用户确认，不替作者作关键判断。
- 文章必须形成连续论述。每节至少包含问题或命题、关系表达、解释与通向下一节的连接，不能退化成标题、公式和短句的列表。
- 简洁指删除冗余，不指删除必要推理。保留让读者理解“为什么从上一句走到下一句”的最短文字。
- 公式用于定义状态、映射、组合、不变量或误差；首次出现的符号必须在附近得到定义。
- 两条路径需要比较时使用交换图；时间顺序或反馈循环使用流程图；并行对应关系使用表格。
- 空间、身体动作或难以用语言精确描述的行为，可以使用有语义的字符画。字符画必须参与论证，不作孤立装饰。
- 每个公式或图表前交代它回答的问题，后面写出由它得到的结论或操作后果；正文不逐字复述图表。
- 图表密度不能打断文章节奏。连续图表之间必须有说明其关系的正文。
- 例子只降低抽象难度，不发展额外情节；例子中的对象与理论对象保持一一对应。
- 保留作者的技术语气与中英文术语选择，同时统一同一概念在全文中的名称。

## 6. 实施顺序

1. 先定义内容数据。
2. 建立单一索引流。
3. 添加内容类型筛选。
4. 添加关系箭头或连接线。
5. 添加一个小型情感元素。
6. 检查桌面端、移动端、键盘和公式渲染。

## 7. 提交前检查

- 删除无语义的线条和图形。
- 删除重复文字与装饰性说明。
- 确认新增内容不需要修改页面结构。
- 确认所有交互无需鼠标也能完成。
